//- Pug file for index.html

//- Import layout. This needs to be done for all views
extends ../partials/layout.pug

block header

block sidebar

block content
  main.join-main

    form.join-form(action="/chat.html")
      h2.heading-secondary.u-margin-bottom-small Join Page

      p.u-margin-bottom-small
        label(for="username").label User Name
        input(type="text" name="username" placeholder="User Name" required).text-input

      p.u-margin-bottom-small
        label(for="room").label Room
        input(type="text" name="room" placeholder="Room" required).text-input

      p
        button.btn Join